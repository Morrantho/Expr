log.c:
* No longer a singleton. Base pointer owned by app like everything else.
	I wanted clean call sites, but it's not worth it at all. Ownership
	issues already arose, and we're not gonna have that.

lexer.c:
* Takes the log pointer on init as a dependency. App owned.

app.c:
* Updated log calls to pass the app's LogList.