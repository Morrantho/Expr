next steps:
* implement interning. Allow tokens to store internid, or number.
* begin parser work.

log.c:
* Renamed: LogFlush to LogDump.
* Removed: LogIsFatal, and log.fatal.
	These just dont need to exist. LogDump( ) now returns non-zero if
	LOG_FATAL was seen. Less state and cleans up call sites.
* Log level now prints before src, column, and line for clarity.

lexer.c:
* Added: lexer.tk is zero initialized.