NumBits: <( n )>
	?( Type( n ) != "number" )
		Print( "N must be a number" )
		<- -1
	::

	bits: 0
	;;
		?( !n ) <== ::
		n >>= 1
		bits++
	::
	<- bits
::

Print( NumBits( 123 ) )
Print( NumBits( "abc" ) )